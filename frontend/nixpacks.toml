[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.build]
cmds = [
  "echo '=== Environment Check ==='",
  "echo NEXT_PUBLIC_API_URL=$NEXT_PUBLIC_API_URL",
  "rm -rf .next",
  "npm run build",
  "echo '=== Build Complete ==='"
]

