[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.build]
cmds = [
  "echo Building with NEXT_PUBLIC_API_URL=$NEXT_PUBLIC_API_URL",
  "NEXT_PUBLIC_API_URL=https://invoiceme-backend.up.railway.app/api npm run build"
]

