[phases.setup]
nixPkgs = ["nodejs_20"]
aptPkgs = []

[phases.build]
cmds = [
  "echo '=== Building with NEXT_PUBLIC_API_URL=$NEXT_PUBLIC_API_URL ==='",
  "npm run build"
]

[build]
cacheDirectories = []

